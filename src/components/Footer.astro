---
import { global } from "../strings";
import chili from "../../assets/chili.svg";
import { Image } from "astro:assets";

interface Props {
  variant?: "default" | "sidebar";

  class?: string;
}

const { variant = "default", class: className, ...props } = Astro.props;
---

<footer class:list={["footer", className, variant === "sidebar" ? "footer-sidebar" : "footer-default"]} {...props}>
  {global.footer.preImage}
  <Image src={chili} alt={global.footer.imageAlt} />
  {global.footer.postImage}
</footer>

<style>
  .footer {
    height: var(--footer-height);
    text-align: center;
    color: var(--color-white);
    font: var(--font-footer);

    img {
      display: inline-block;
      height: 2.5rem;
      margin: 0 0 0.5rem -0.5rem;
      vertical-align: middle;
    }
  }

  .footer-default {
    align-self: flex-start;
    background-color: var(--color-footer);
    width: 100vw;
  }

  .footer-sidebar {
    padding-bottom: 0.75rem;
    width: 100%;
  }
</style>
